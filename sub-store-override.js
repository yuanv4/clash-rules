// Generated by scripts/build.mjs; do not edit.
// Consumed by the DS420 sub-store mihomoConfig override (Script Operator, mode=link).
async function main(config = {}) {
  const withTailscale = ($file && ($file.name || '').indexOf('tailscale') !== -1);

  // cloud-hk 节点:去重 + 追加末尾。凭据存于 sub-store 本地文件,不进入仓库。
  const cloudHk = JSON.parse(await produceArtifact({ type: 'file', name: 'cloud-hk-node' }));
  config.proxies = config.proxies || [];
  config.proxies = config.proxies.filter((p) => !(p && p.name === cloudHk.name));
  config.proxies.push(cloudHk);

  const names = config.proxies.map((p) => p.name);
  const aiRule = /(?:SG|SGP|Singapore|新加坡|🇸🇬)/i;
  const aiNames = names.filter((n) => aiRule.test(n));
  config['proxy-groups'] = [
    { name: '⚡ 自动选择', type: 'url-test', url: 'https://cp.cloudflare.com/generate_204', interval: 300, tolerance: 50, proxies: names.slice() },
    { name: '🤖 AI', type: 'url-test', url: 'https://cp.cloudflare.com/generate_204', interval: 300, tolerance: 50, proxies: aiNames },
    { name: '🌐 国内', type: 'select', proxies: ['DIRECT', '⚡ 自动选择'] },
    { name: '🐟 漏网之鱼', type: 'select', proxies: ['⚡ 自动选择', 'DIRECT'] },
  ];

  config.mode = 'Rule';
  config['log-level'] = 'info';
  config['rule-providers'] = {"lan_non_ip":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/lan_non_ip.yaml"},"lan_ip":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/lan_ip.yaml"},"reject_non_ip":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/reject_non_ip.yaml"},"reject_ip":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/reject_ip.yaml"},"ai":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/ai.yaml"},"direct":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/direct.yaml"},"tag":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/tag.yaml"},"foreign":{"type":"http","behavior":"classical","format":"yaml","interval":86400,"url":"https://raw.githubusercontent.com/yuanv4/clash-rules/release/rules/foreign.yaml"}};
  config.rules = ["RULE-SET,lan_non_ip,DIRECT,no-resolve","RULE-SET,lan_ip,DIRECT,no-resolve","RULE-SET,reject_non_ip,REJECT,no-resolve","RULE-SET,reject_ip,REJECT,no-resolve","RULE-SET,ai,🤖 AI,no-resolve","RULE-SET,direct,🌐 国内,no-resolve","RULE-SET,tag,🌐 国内,no-resolve","RULE-SET,foreign,⚡ 自动选择","MATCH,🐟 漏网之鱼"];

  if (withTailscale) {
    const secret = JSON.parse(await produceArtifact({ type: 'file', name: 'tailscale-secret' }));
    config.proxies = config.proxies.filter((p) => !(p && p.name === 'TAILSCALE'));
    config.proxies.unshift({
      name: 'TAILSCALE', type: 'tailscale',
      hostname: secret.hostname,
      'auth-key': secret['auth-key'],
      'control-url': secret['control-url'],
      'state-dir': secret['state-dir'],
      ephemeral: !!secret.ephemeral,
      udp: secret.udp !== false,
      'accept-routes': secret['accept-routes'] !== false,
      'ip-version': secret['ip-version'] || 'ipv4-prefer',
    });
  }
  config['proxy-groups'].unshift({ name: 'Tailscale', type: 'select', proxies: withTailscale ? ['TAILSCALE', 'DIRECT'] : ['DIRECT'] });
  config.rules = ["IP-CIDR,100.64.0.0/10,Tailscale,no-resolve","IP-CIDR,100.100.100.100/32,Tailscale,no-resolve","DOMAIN-SUFFIX,ts.net,Tailscale"].concat(config.rules);

  return config;
}
